# Practices

1. Write a function called `getMinBaseStats` that:

   1. Receives an array of `Pokemon` (see types [here](./datasets/Pokemon.ts))
   2. Returns an object with the minimum base stat of all Pokemon (the minimum value for each stat)

## Links

- [Answer](1.2.answer.test.ts)
